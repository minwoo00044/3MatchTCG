# CLAUDE.md

Unity 6 기반 3매치 퍼즐 + TCG 프로젝트.

## 먼저 읽을 것

작업을 시작하기 전에 **[AGENTS.md](AGENTS.md)를 읽는다.**

프로젝트의 구조, 네이밍, 핵심 패턴, 그리고 이 저장소에서 실제로 터졌던 버그에서 나온 규칙들이 정리되어 있다.
특히 다음은 어기면 같은 버그를 다시 만들게 되는 항목이다.

- **영수증 패턴** — 상태를 바꾸는 함수를 부르기 *전에* 필요한 참조를 확보한다 (같은 원인으로 세 번 물렸음)
- **FSM 상태 클래스 이름** — `{Owner}{State}State`. 리플렉션으로 자동 등록되므로 틀리면 조용히 동작하지 않는다
- **풀 반납 시점** — 연출이 끝난 뒤에 반납한다. 반납 주체는 한 곳
- **판정 규칙 단일화** — 같은 질문에 답하는 함수를 두 개 만들지 않는다
- **연출은 DOTween으로 통일** — 특별한 사유가 없는 한 다른 수단을 섞지 않는다

`AGENTS.md`의 모든 규칙은 **최적화·유지보수성·확장성 등 합당한 이유가 있으면 예외로 처리할 수 있다.**
다만 어기기 전에 이유를 말하고, 왜 예외인지를 주석에 남긴다.

## 작업을 이어받는 경우

**[docs/HANDOFF.md](docs/HANDOFF.md)를 읽는다.**

지금까지 무엇이 되어 있고 무엇이 비어 있는지, 남은 작업의 선행 순서, 그리고 이미 세워둔 가정이 정리되어 있다.
게임 기획은 **[docs/GDD.md](docs/GDD.md)**가 단일 원천이며, **이 문서는 기획자 소유다.**
기획에 손댈 일이 생기면 직접 고치지 말고 제안한다.

## 빌드 / 실행

Unity 에디터에서 연다. 이 저장소에는 Unity 참조 어셈블리가 없으므로
**에이전트는 컴파일 여부를 검증할 수 없다.** 코드를 수정한 뒤에는 항상
"에디터에서 컴파일 확인이 필요하다"고 명시한다.

## 컨벤션 갱신

작업 중 컨벤션으로 굳힐 만한 판단이 나오면 `AGENTS.md`에 임의로 추가하지 말고
**사용자에게 제안한다.**
